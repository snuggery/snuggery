{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Create yarn version",
	"description": "Target options for creating versions using yarn",
	"type": "object",
	"properties": {
		"prerelease": {
			"oneOf": [{"type": "string"}, {"type": "boolean"}],
			"description": "Add a prerelease identifier to new versions"
		},
		"dryRun": {
			"type": "boolean",
			"description": "Only print what would happen, don't actually deploy anything"
		}
	},
	"additionalProperties": false
}
